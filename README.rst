json_writer
===========

This project is intended to be a proof of concept for an iterative JSON
generation in Python. As is the API is probably not friendly to users, and
should really only be used as the basis for a broader API generation framework.

